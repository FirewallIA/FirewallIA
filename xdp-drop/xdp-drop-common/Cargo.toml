# Fichier : /root/FirewallIA/xdp-drop/xdp-drop-common/Cargo.toml

[package]
name = "xdp-drop-common"
version = "0.1.0"
edition = "2021"

[dependencies]
# Bytemuck est la dépendance clé pour Pod
bytemuck = { version = "1", features = ["derive"] }

[lib]
# Le crate-type doit être une lib rust normale, pas cdylib
crate-type = ["lib", "rlib"]